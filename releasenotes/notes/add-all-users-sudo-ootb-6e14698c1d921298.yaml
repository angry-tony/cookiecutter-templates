---
others:
  - Enabled any defined user to reach the root shell, by default.
    To limit the shell access in production environments, set the
    ``linux_system_user_sudo`` to ``False`` in the ``infra/init.yml``
    file of your Reclass model.
