parameters:
  linux:
    system:
      repo:
        tcpcloud:
          default: true
          source: "deb [arch=amd64] {{ cookiecutter.apt_repository }}/{{ cookiecutter.apt_branch }}/ {{ cookiecutter.apt_release_code }} {{  cookiecutter.apt_repo_tags|default('main security extra') }}"
          architectures: amd64
          key_url: "{{ cookiecutter.apt_repository }}/public.gpg"
