parameters:
  nginx:
    server:
      enabled: true
      site:
        nginx_proxy_graphite:
          enabled: true
          type: nginx_proxy
          name: graphite
          proxy:
            host: ${_param:mtr_vip_address}
            port: 80
            protocol: http
          host:
            name: ${_param:cluster_public_host}
            port: 8080
