classes:
- system.ceph.mon.cluster
- system.ceph.monitoring.node_stats
{%- if cookiecutter.ceph_version == 'luminous' %}
- service.ceph.mgr.cluster
{%- endif %}
- cluster.{{ cookiecutter.cluster_name }}.ceph.common
parameters:
  linux:
    network:
      interface:
        ens3: ${_param:linux_single_interface}
